/*const { GtaConService } = require('./conexionApi');

class GtaService {
  #infoGta = new GtaConService();

  async connectToDestination() {
    return this.#infoGta.infoSupplier(`ZWFIC003Set('${}')?$format=json`);
  }
}

module.exports = { GtaService };*/